{
  "name": "remoteload",
  "version": "0.0.1",
  "description": "Library to load files from a url into a local temp directory for local use.",
  "main": "src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "remote",
    "load",
    "file",
    "local"
  ],
  "author": "Thomas Lanteigne",
  "license": "ISC",
  "dependencies": {
    "rimraf": "^2.6.2"
  }
}
